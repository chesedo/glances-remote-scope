# Below are some example settings. You can access your scope's settings by
# calling scopes.self.settings from anywhere in your scope implementation.
# E.g. var location = scopes.self.settings["location"].get_string();

[ip]
type = string
_displayName = Remote IP

[port]
type = string
_displayName = Port
defaultValue = 61208

